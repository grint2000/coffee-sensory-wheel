# 커피 평가 앱 조사와 가안 반영

이후 수정과 최신 검증 범위는 [미완료 항목 재점검](RELEASE_READINESS.md)을 참조한다.

확인일: 2026-09-08. 대상: `grint2000/coffee-sensory-wheel`, 가안 `codex/flavor-molecule-rank-v2`.
기준 커밋: `741439b58536468a8dc82a8ba07fd722e19dbea6`.
수정 전 백업: `backup/before-app-workflow-20260908`.
공개 main 기준: `984f907235a1e63cb3782bf40a2244194042c43d`.

## 조사한 기능과 선정 이유

두 앱의 공식 사용 안내를 직접 열람했다. 유료 계정으로 앱에 로그인해 조작한 것은 아니다. 가격·우열·시장점유율은 이번 선정 근거로 사용하지 않았다.

| 공식 자료 | 확인한 흐름 | 이번 앱에 적용한 방식 |
|---|---|---|
| [Tastify, 2023 사용 안내](https://www.tastify.com/article/how-to-use-tastify) | 기록 검색, 2~4개 샘플 비교, 타이머, 평가자별 결과 확인 | 전체 세션 검색·날짜 필터·정렬·최대 4개 비교, 세션 타이머 |
| [Cropster, 2026 평가 입력 안내](https://help.cropster.com/en_US/cupping/how-to-add-cupping-scores) | 세션 준비 후 평가, 샘플 또는 평가자별 결과 묶기 | 평가 조건 재사용, 기존 팀 결과에 평가자별 총점·메모 추가 |

‘같은 조건 새 세션’은 Cropster의 세션 준비 흐름을 참고해 이 앱의 반복 QC 업무에 맞게 설계한 기능이다. Cropster에 동일한 복제 버튼이 있다는 주장은 아니다. 외부 앱의 코드·상표 그래픽·향미 데이터는 복사하지 않았다.

현재 계정의 과거 입고·로스팅 기록을 찾고 비교하는 기능을 우선했다. 팀 평균·평가자 보정은 서로 다른 평가가 같은 로트·같은 평가 조건인지 연결하는 식별자가 부족해 추가하지 않았다. 서로 다른 커피의 점수를 평균내는 오류를 피하기 위한 범위 결정이다. 점수 체계나 향미분자 연구 결과도 이번 조사로 변경하지 않았다.

## 사용 방법

1. **전체 기록 검색·비교**를 열어 커피명·로트·생산자·향미 또는 메모의 단어를 검색한다. 여러 단어를 입력하면 모두 포함하는 기록을 찾는다. 평가일 범위와 정렬을 바꿀 수 있다.
2. 서로 다른 세션에서 2~4개 기록을 선택해 비교한다. 비교 열에 날짜와 세션명이 함께 표시된다. **열기**는 해당 세션의 원래 샘플로 이동하며 기록을 복제하지 않는다.
3. **같은 조건 새 세션**은 저장된 목적·장소·평가자·수온·분쇄도를 재사용한다. 날짜·시간·샘플은 새로 만들고 기존 점수·향미는 복사하지 않는다. 새 샘플에는 기존 앱의 초기 점수 7이 들어가므로 평가 완료 점수로 해석하면 안 된다.
4. **세션 타이머**는 시작·일시정지·초기화를 지원한다. 계정과 세션별 기기 저장이며 재접속 시 이어진다. 경과 시간은 기기 시계를 기준으로 하므로 시계를 바꾸면 영향을 받는다. 특정 커핑 단계의 공식 시간이나 알람 기능은 아니다.
5. **팀 결과 보기**에서는 평가자별 기록 총점·향미·메모를 확인한다. 공유 방식은 기존 가안과 동일한 명시적 세션 공유이다.

## 발견한 문제와 수정

- 세션 선택 후 선택 메타데이터를 쓰지 않아 재접속 시 과거 세션이 열릴 수 있었다. 선택 시 세션 ID를 저장한다.
- 저장 실패를 무시하고 세션/샘플을 바꿀 수 있었다. 저장에 실패하면 이동을 중단한다.
- 세션 생성 실패 시에도 생성 성공을 표시할 수 있었다. 백업과 저장 성공 후에만 새 세션을 활성화하며 실패를 안내한다.
- 복제 샘플의 외부 이름과 내부 평가 데이터의 이름이 달라 다음 저장에서 원래 이름으로 돌아갔다. 두 이름을 일치시킨다.
- JSON에 샘플 이름은 있지만 내부 title이 없으면 입력창이 비었다. 가져오기 정규화 시 샘플 이름을 내부 title에도 반영한다. 원래 JSON 파일은 변경하지 않는다.
- README의 이름 입력 로그인·자동 팀 동기화·별도 전체 보고서 이미지 버튼 설명이 현재 코드와 달랐다. 실제 동작과 남은 제한에 맞게 수정했다.
- 새 검색창이 열린 동안 뒤쪽 화면의 단축키가 작동하지 않게 했다. 닫기와 Escape는 기본 dialog 동작을 사용한다.

## 구조와 검증

추가 기능: `workflow-tools.js`, `workflow-tools.css`. 기존 평가 데이터 접근은 `NOEL_WORKFLOW_API`를 통해 연결한다. 과거 기록이나 새 타이머 데이터를 다른 서비스로 전송하지 않는다. 서비스 워커 v17의 필수 설치 목록에 새 코드·스타일을 포함한다. 시험 페이지는 새 타이머도 별도 시험 저장소 접두사로 격리한다.

| 검증 | 결과와 범위 |
|---|---|
| `tests/workflow-tools.cjs` | 검색·다중 검색어·향미·날짜 범위·정렬·원본 비변경, 타이머 재접속/일시정지/초기화/계정·세션 분리 통과 |
| `tests/workflow-integration.cjs` | 실제 앱 스크립트를 jsdom 가상 DOM과 시험 데이터로 실행. 입력·저장·복제·실행취소·조건 재사용·타이머 버튼·검색·비교·원기록 열기·선택 저장·저장 실패·점수 계산·FileReader JSON 가져오기·복원·다크모드 통과 |
| 기존 회귀 검증 | 저장/백업, 오류 경로, 모의 Firebase, 모의 캐시, 전체 향미 데이터 감사 통과 |
| JavaScript/정적 파일 | 소스 문법 및 참조 파일 확인 |
| 실제 브라우저 | 연결된 브라우저의 탭 갱신과 기존 확인창 제어가 각각 20초 시간 초과. 연결을 복구하지 못함 |
| 남은 실기 검증 | 모바일 화면·네이티브 dialog 키보드/초점·실제 다운로드/설치, Google 인증·Firebase 허용 도메인/서버 규칙·두 기기 팀 공유 |

가상 DOM은 화면 배치·기기 터치·브라우저 다운로드·인증 성공을 증명하지 않는다. 실제 사용자 데이터로 팀 쓰기를 테스트하지 않았다. 이번 기능 구현과 자동 검증 완료를 전체 운영 검증 완료로 보고하지 않는다.

향미 사전 감사 결과는 전체 420개 중 순위 확인 5개, 주요 분자 확인 23개, 추가 조사 387개, 적용 제외 5개로 유지된다. 향미 연구의 전체 완료와도 별개이다.
