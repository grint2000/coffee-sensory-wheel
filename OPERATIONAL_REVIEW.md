# 운영 가안 수정 기록 — 2026-09-08

공개 main에 반영하지 않은 운영 개선입니다. **개인 기기 기록의 저장·복구를 우선 수정했으며 팀 공동 운영 전체의 준비 완료를 뜻하지 않습니다.**

## 복구 기준

- 작업 전 가안: `07d11f180463c225343cd32cfbaf180d537e2aaf`
- 백업 브랜치: `backup/before-operational-fixes-20260908`
- main 기준: `984f907235a1e63cb3782bf40a2244194042c43d`
- main 백업: `backup/main-before-reference-20260908`
- 작업 브랜치: `codex/flavor-molecule-rank-v2`

## 수정 내용

1. 현재 세션 JSON(객체), 전체 세션 JSON(배열), 이전 샘플 JSON(배열)을 구분하여 불러옵니다. 기존 내보내기 파일 형식은 유지합니다.
2. 가져오기 전에 식별자·중복·세션·샘플·향미 목록 구조를 검증합니다. 검증을 통과한 뒤 현재 기록을 백업하고 저장한 후 화면을 바꿉니다. 같은 식별자가 있는 파일을 가져올 때 이전 편집창 내용이 새 기록에 섞이지 않게 했습니다.
3. 전체 세션 저장을 기준으로 성공 여부를 반환합니다. 저장 실패에도 성공 토스트나 저장됨 표시가 나오던 코드를 제거하고 상태를 계속 표시합니다. 저장되지 않은 기록은 창을 닫기 전에 JSON으로 내보낼 수 있습니다.
4. 읽지 못하는 기존 원본은 자동으로 새 빈 기록으로 덮어쓰지 않습니다. `저장된 원본 파일 백업`으로 파싱하지 않은 원본을 보존할 수 있습니다. 올바른 JSON을 불러오면 교체 전 원본도 복구 사본에 포함합니다.
5. 백업은 사용자별로 분리합니다. 백업 쓰기 실패 시 기존 사본을 삭제하지 않습니다. 새 자동 백업 성공 후 해당 사용자의 자동 백업만 최근 7개 유지합니다. 가져오기·복원·실행 취소 전 사본과 이전 버전 공용 백업은 자동 삭제하지 않습니다. 공용 백업에는 사용자 미확인 표시가 붙습니다.
6. 반복 오류를 이유로 사용자의 현재 기록을 과거 백업으로 자동 대체하는 동작을 제거했습니다.
7. 재열기 시 마지막 세션·샘플을 복원합니다. 실행 취소는 전체 세션 구조를 사용합니다. 실행 취소 전용 상태 API를 사용하며 팀 스크립트에 새로 데이터를 노출하지 않습니다.
8. 로컬 편집 기록을 불러온 사용자와 현재 사용자가 다르면 기기 저장을 중단하여 다른 사용자 이름으로 기록이 저장되지 않게 했습니다. 로그인·로그아웃·팀 동기화 함수는 기존 상태로 유지했습니다. 계정 전환과 서버 왕복은 별도 승인·검증이 필요합니다.
9. 시간의 콜론 등 엑셀 시트명에 사용할 수 없는 문자를 처리합니다. 현재 세션을 내보낼 때 편집창 값을 먼저 반영합니다. 차트·드래그·이미지·엑셀 외부 라이브러리가 없을 때 해당 기능이 기본 기록 흐름 전체를 중단시키지 않도록 일부 방어 처리를 추가했습니다.
10. 캐시 v14에 새 data-store.js를 포함하고 페이지·사전·저장 모듈·보조 스크립트를 필수 설치 대상으로 지정했습니다.

향미 사전 데이터와 점수 산식은 이번 작업에서 변경하지 않았습니다. 실제 운영 사용자 기록이나 Firebase 서버 데이터도 직접 수정하지 않았습니다.

## 검증 결과

- `node tests/data-store.cjs`: 통과. 세 가지 JSON 형식, 잘못된 구조·중복 거부, 원본 불변, 사용자별 백업 분리, 백업/저장 실패, 동일 식별자 교체, 손상 원본 덮어쓰기 차단, 현재 세션 복원, 사용자 전환 보호, 엑셀 시트명 처리를 검증했습니다. 저장소와 엑셀 API는 모의 구현입니다.
- `node tests/service-worker.cjs`: 통과. 모의 캐시에서 설치·구버전 정리·오프라인 대체·실패한 설치를 검사했습니다.
- `node scripts/audit-reference.cjs`, 구문 검사: 통과. 전체 향미 420개와 기존 분자 정보 개수는 그대로입니다.
- 브라우저에서 확인: 실제 앱 코드를 별도 시험 저장소에서 실행해 샘플명·메모 입력, 딸기 선택, 기기 저장, 다시 열었을 때 샘플·향미 선택 유지, 복제 후 실행 취소를 확인했습니다. 팀 연결과 PWA 등록은 시험에서 제외했습니다.
- 브라우저 확인 한계: 단일 세션 JSON 파일을 실제 파일 입력 이벤트로 전달한 뒤 열린 네이티브 확인창에서 자동 제어가 시간 초과됐습니다. 따라서 가져오기 확인·복원 버튼의 브라우저 시험은 완료하지 못했습니다. 이후 엑셀 보완은 코드 검사와 회귀 시험으로 검증했습니다.
- 미검증: 실제 Google 로그인·Firestore 팀 왕복 동기화·동시 편집·서버 접근 규칙, 실제 휴대전화 전체 화면 및 터치, 설치된 PWA의 실제 캐시 갱신, 브라우저에서 다운로드한 XLSX 파일 재열기. 팀 동기화의 기존 세션/샘플 저장 구조 차이도 별도 검토가 필요합니다.

## 사용 순서

1. 가안에서 시험용 세션과 샘플을 만들고 이름·향미·점수를 입력합니다.
2. `이 기기에 전체 저장` 후 저장 상태를 확인합니다. 이는 해당 브라우저의 저장이며 팀 서버 저장 완료 안내가 아닙니다.
3. 종료 전 `JSON 내보내기`로 전체 기록을 파일에 보관합니다. `현재 세션 내보내기`는 선택 세션만 보관합니다.
4. 파일을 불러오거나 백업을 복원하면 현재 기록을 먼저 백업합니다. 파일 검증 또는 저장이 실패하면 기존 기록 교체를 중단합니다.
5. 저장 오류가 뜨면 창을 닫기 전에 JSON을 내보냅니다. 원본 읽기 오류라면 `저장된 원본 파일 백업`도 보관합니다.

공개 반영과 팀 공동 운영의 준비 완료는 아직 승인하거나 주장하지 않았습니다.

## 확인한 공식 규칙

- Microsoft, 2026-09-08 확인: [워크시트 이름 제한](https://support.microsoft.com/en-us/excel/rename-a-worksheet).
- WHATWG HTML Living Standard, 2026-09-08 확인: [Web Storage 저장 실패와 예외](https://html.spec.whatwg.org/multipage/webstorage.html).

## 자동 승인 검토에서 중단된 범위

로그인·팀 동기화 변경 요청은 자동 승인 검토에서 거부됐습니다. 기존 요청의 로그인·팀 기능/사용자 데이터 변경 제한 및 외부 Firebase 프로젝트 `coffee-sensory-app`으로 기록이 전송될 수 있다는 이유입니다. 해당 team.js 변경은 저장소에 반영되지 않았습니다. 로그인 보조 함수도 원래 상태로 유지했습니다. 팀 코드에 연결하는 추가 상태 노출도 제거했습니다.

추가 작업에는 이 Firebase 프로젝트의 소유·사용 권한 확인과 로그인·팀 동기화 수정 범위 승인이 필요합니다. 검토할 구체적 범위는 로그인 전후 로컬 사용자 전환, 세션과 서버 샘플 구조의 호환성, 동기화 실패 표시, 다중 사용자 충돌 및 서버 접근 규칙입니다. 실제 사용자 기록을 시험 데이터로 사용하지 않습니다.
