# 미완료 항목 재점검 및 수정 기록

확인일: 2026-09-08. 작업 브랜치: `codex/flavor-molecule-rank-v2`.
이 문서가 이전 운영 검토 문서의 현재 상태를 보완한다. 자동 검증 통과는 전체 실서비스 검증 완료를 뜻하지 않는다.

## 기준과 보존

- 수정 전 가안: `a923b95eef46d05ce71a16e979f0a4aa4f2ba643`
- 복구 브랜치: `backup/before-release-recovery-20260909` (위 커밋을 가리키는 실제 브랜치명)
- 공개 main 기준: `984f907235a1e63cb3782bf40a2244194042c43d`
- 기존 main 백업: `backup/main-before-reference-20260908`
- 이번 변경은 가안에만 적용한다. 점수 산식, 향미 연구 데이터, 실제 사용자 기록 및 Firebase 서버 설정은 변경하지 않는다.

## 확인한 원인과 해결

| 항목 | 확인한 원인 | 변경 및 확인 |
|---|---|---|
| Excel 합계·결점 수 | 합계가 문자열로 저장되고 숫자 0이 빈 칸으로 바뀌었다 | 숫자 합계와 소수 둘째 자리 표시 형식, 결점 수 0 유지. 실제 XLSX 바이너리 생성 후 다시 읽어 한글·80.5·0·시트명 확인 |
| SNS 이미지 실패·재시도 | PNG 변환 실패 경로의 임시 DOM 정리가 빠졌고 중복 실행이 가능했다 | 성공·렌더 실패·PNG 인코딩 실패 모두 정리, 처리 중 버튼 잠금. 브라우저에 다운로드를 요청한 사실만 안내 |
| 오프라인 라이브러리 | 외부 의존성이 최초 설치의 필수 목록에 없었다 | 기존과 같은 버전의 차트·XLSX·이미지·드래그·스타일·아이콘 폰트를 앱과 함께 필수 캐시 |
| 캐시 범위 | 광범위한 같은 출처 및 Google API GET을 캐시할 수 있었다 | 앱 파일과 지정 라이브러리만 대상으로 제한. Firebase/Auth/Firestore 요청 제외 |
| 다른 경로 캐시 삭제 | 공통 캐시 이름과 정리 조건으로 다른 설치 경로에 영향을 줄 수 있었다 | 설치 경로별 v18 캐시, 같은 경로의 구버전만 정리. 경로 없는 옛 캐시는 다른 탭 보호를 위해 보존 |
| 네트워크 대기 | 캐시 대체 전에 응답 없는 요청을 계속 기다릴 수 있었다 | 설치된 앱은 캐시에서 바로 제공. 캐시 없는 탐색의 네트워크 대기는 4초 후 오프라인 안내. 필수 파일 설치 실패 시 이전 워커 유지 |
| 로그인 오류 식별 | 오류가 원인별로 일관되게 표시되지 않았다 | 허용 도메인·팝업 차단/취소·Google 공급자 미활성·연결·팀 접근 오류를 한국어로 구분하고 상태 영역에 유지 |
| 팀 조회·중복 로그인 | 단건 조회에 대기 한계가 없고 로그인 반복 클릭을 막지 않았다 | 단건 읽기 12초 제한, 처리 중 로그인 중복 억제. 쓰기에 임의의 시간 제한이나 자동 재전송을 추가하지 않음 |
| 검증 재현 | 임시 설치 경로와 개별 명령에 의존했다 | 개발용 의존성 버전·잠금 파일, `npm test` 실행기, 가안 전용 GitHub Actions 검사 구성 |

캐시는 성공적으로 설치된 앱 파일을 제공한다. Firebase 로그인·팀 서버 기능의 오프라인 성공을 보장하지 않는다. 첫 설치의 외부 파일 다운로드와 선택적 Google 글꼴은 네트워크 조건의 영향을 받는다. 실제 설치 및 구버전 PWA에서의 전환은 별도 실기 항목이다.

## 재현 가능한 검사

```sh
npm ci --ignore-scripts --no-audit --no-fund
npm test
```

개발 의존성은 jsdom 26.1.0과 현재 앱이 사용하는 XLSX 0.18.5이다. 정적 앱 실행에 npm 서버는 필요하지 않다. 검사는 합성 데이터와 격리된 메모리 저장소를 사용하며 실제 사용자 팀에 쓰지 않는다.

| 검사 | 검증 범위 |
|---|---|
| `tests/data-store.cjs` | JSON 형식·손상 원본 보호·백업·쓰기 실패·프로필 및 세션 복원 |
| `tests/feature-failures.cjs` | 실행 취소 실패·계정 분리·인증 모듈 부재·사용자 입력 이스케이프·단축키 저장 실패 |
| `tests/workflow-tools.cjs` | 전체 기록 검색·날짜·정렬·원본 불변, 타이머 재접속·중단·초기화·분리 |
| `tests/workflow-integration.cjs` | 실제 앱 스크립트의 가상 DOM 입력·저장·복제·취소·반복 조건·검색·비교·JSON FileReader·복원·테마. 실제 XLSX 코덱으로 파일 생성/재열기. 모의 이미지 렌더러로 성공 및 실패 후 정리 |
| `tests/reference-dom.cjs` | 실제 향미 모달 코드: 탭·버튼·600ms 롱프레스·이동/취소/이탈, 420개 제목·최대 3개·순위 표현·접힌 상세·출처 URL·초점 복원. DOM/포인터 시뮬레이션 |
| `tests/team.cjs` | 모의 Firebase 계정 분리·중복 팀·재가입·취소·동시 변경·명시적 공유·로그아웃·로그인 중복·조회 제한·오류 설명 |
| `tests/service-worker.cjs` | 모의 Cache API 필수 설치·오프라인 앱·캐시 분리·API 제외·응답 지연/5xx·설치 실패 |
| `scripts/audit-reference.cjs` | 실제 최신 420개 목록·정확한 키·최대 3개·순위 구조·출처 필드·스크립트 문법. 원문 내용 검증 또는 링크 접속 검사는 아님 |

로컬 전체 실행은 통과했다. 원격 Actions의 실제 실행 상태는 해당 커밋의 **Actions → Draft regression checks**에서 확인한다. 이 검사는 공개 배포 작업을 포함하지 않는다.

## 해결 완료로 표시할 수 없는 항목

| 항목 | 실제 확인 결과 | 완료에 필요한 확인 |
|---|---|---|
| 실브라우저 조작 | 이전 네이티브 확인창 제어와 이번 새 탭 생성 모두 CDP 탭 갱신에서 20초 시간 초과. 새 탭 ID를 받기 전 실패 | 사용 가능한 브라우저 연결에서 실제 조작. 이것만으로 앱의 자바스크립트나 호스팅이 원인이라고 진단할 수 없음 |
| Google/Firebase 운영 설정 | 관리 설정·배포된 보안 규칙을 읽지 못함. 공개 프로젝트 설정 읽기도 네트워크 승인 취소로 요청 완료 여부 확인 불가 | 허용 도메인, Google 공급자, 실제 배포 규칙을 관리 접근으로 확인하고 시험 계정 2개로 가입·공유·충돌·제거 확인. 사용자에게 이미 받은 수정 승인을 다시 받을 사안은 아님 |
| 모바일·다운로드·PWA | 가상 DOM과 실제 XLSX 코덱 검증은 완료. 물리 터치·레이아웃·네이티브 다운로드·PNG 픽셀·설치/업데이트는 미검증 | 연결된 실제 브라우저/기기에서 파일과 화면 결과 확인 |
| 향미 문헌 | 420개 목록 감사 완료. 순위 5개, 순위 없는 주요 분자 23개, 추가 조사 387개, 제외 5개 | 387개의 대상·상태별 원문 조사. UI 검사 통과가 과학적 데이터 완성을 뜻하지 않음 |
| 공개 반영 | main을 변경하지 않는 가안 범위 | 공개 반영에 대한 별도 승인. 기존 Firebase 기능 수정 승인은 공개 배포 승인으로 확대하지 않음 |

앱을 아직 열 수 없는 환경에서 Firebase 도메인이나 보안 규칙이 잘못됐다고 단정하거나 관리 설정을 추측으로 덮어쓰지 않는다. 외부 연결 및 원문 조사가 필요한 항목을 자동 검사 통과로 대체해 전체 완료라고 표시하지 않는다.

기술 참고: [Firebase Google 로그인](https://firebase.google.com/docs/auth/web/google-signin), [Firebase Auth REST](https://firebase.google.com/docs/reference/rest/auth), [Identity Platform 관리 설정 접근](https://docs.cloud.google.com/identity-platform/docs/reference/rest/v2/projects/getConfig), [네트워크 대기 제한과 오프라인 대체](https://developer.chrome.com/docs/workbox/forcing-a-network-timeout). 이 앱은 Workbox 라이브러리를 사용하지 않는다.
